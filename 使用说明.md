# QAnimator Encoder · Unity 6

这是在原半成品上完善的独立动画编码软件，按 Leviathan 类工具的工作方式组织。保留项目名称 QAnimator，输出真实的 QANM 自定义 `.bytes` 动画。

## 直接使用

双击根目录 **启动编码器.cmd**，或运行 `dist/QAnimatorEncoder/QAnimatorEncoder.exe`。

本机便携目录已包含 .NET 运行库、FFmpeg 和 ffprobe，无须另外安装 .NET。整个 `dist/QAnimatorEncoder` 文件夹需要一起保留。

1. 添加或拖入 MP4 / WebM。
2. 选择输出目录。FPS 为 0 时采用源帧率取整；宽高均为 0 时保持原尺寸。只设置一个尺寸时等比例缩放。
3. 点击“开始所有任务”。转换期间可停止，结束后可继续未完成任务。
4. 点击“预览 .bytes”，打开生成文件。支持播放、暂停和拖动进度；棋盘格用于检查透明背景。

GUI 遇到同名输出会自动添加 `_2`、`_3` 等后缀，保护已有文件。CLI 成功时替换指定输出；失败或取消时保留已有输出。

普通 MP4 的背景不会自动变透明；要透明动画，请使用有 Alpha 的 WebM。“保留透明通道”关闭后所有像素 Alpha 强制设为 255，RGB 保持不变。

## 已准备的宝箱素材

均位于根目录，512×512、30 FPS、4 秒 / 120 帧：

| 文件 | 用途 |
|---|---|
| `treasure_chest_open.mp4` | 深色背景的开箱动画 |
| `treasure_chest_open.bytes` | MP4 转码结果，可直接预览 / 导入 Unity |
| `treasure_chest_open_alpha.webm` | 透明开箱动画，含柔和阴影、闪光 |
| `treasure_chest_open_alpha.bytes` | 透明动画转码结果 |
| `treasure_chest_contact_sheet.jpg` | 闭合到打开的四个关键画面 |

素材由 `scripts/generate_chest.py` 程序绘制，固定随机种子，可重复生成；不是下载的第三方动画。重新生成需要 Python + Pillow，以及支持 libx264 / libvpx-vp9 的 FFmpeg。

## Unity 6 接入与已完成验证

已接入 `E:/VideoEncode/My project`，Unity 6000.0.61f1 中 25 项播放测试全部通过。打开 `Assets/QAnimatorValidation/QAnimatorValidation.unity` 并进入 Play 模式即可查看宝箱。菜单 **Tools → QAnimator → Run Unity validation** 可重新测试，报告在项目根目录 `QAnimatorTestResults`。

手动接入也可以：

1. Package Manager → 从磁盘添加包，选择本仓库 `Unity/package.json`。也可以把 `Unity/Runtime` 和 `Unity/Editor` 复制到项目 `Assets/QAnimator` 下；两种方法选择一种。
2. 把根目录 `treasure_chest_open_alpha.bytes` 复制到 Unity 的 `Assets` 下。
3. 在 Project 窗口选中该 `.bytes`，点击菜单 **Tools → QAnimator → Create demo from selected .bytes**。
4. 进入 Play 模式。界面提供播放、暂停、停止、重新开始、循环、速度和进度控制，也会显示完成回调次数。

测试界面使用 `RawImage`。Unity 只需要 `.bytes`；运行时自定义解码并复用 `Texture2D`，无视频播放器、系统视频解码器或 FFmpeg。

## 本次修复与增加

- GUI / CLI 共用转换服务，FFmpeg 确认成功后才替换输出。
- 失败、取消、工具启动异常的处理；GUI 关闭时先停止当前转换。
- 批次使用任务快照，运行期间锁定设置和阻止添加，避免队列集合修改异常。
- 编码工作放到后台，进度通知节流，减少界面卡顿。
- 单尺寸等比例缩放、输出重名处理、真正的去 Alpha。
- VP8 / VP9 透明解码选择，明确选择首个视频流。
- `.bytes` 原生预览，共用 Unity 解码器。
- 校验帧索引长度后再分配数组，拒绝伪造的巨大帧数。
- Unity 测试菜单和可选控制面板。

## 复现测试与构建

```powershell
./scripts/test.ps1
./scripts/build-portable.ps1
```

脚本自动寻找 `tools/ffmpeg` 中的工具，也可传 `-FfmpegDirectory '工具的 bin 目录'`。

FFmpeg 来自 FFmpeg 官网下载页列出的 [Gyan Windows 构建](https://www.gyan.dev/ffmpeg/builds/)，本机为 9.0.1 essentials；原始许可证和说明保留在工具目录及便携目录。`tools/ffmpeg`、`dist`、`artifacts` 均忽略 Git 跟踪。

命令行示例：

```powershell
./dist/QAnimatorEncoder/QAnimatorEncoder.exe --input treasure_chest_open_alpha.webm --output demo.bytes --width 256 --fps 30
```

## 已知限制

- Unity 6 Editor 已通过测试；独立构建、Android、iOS、IL2CPP 和设备性能仍需验证。
- 源分数帧率仍取整；音频不编码，定位是短时游戏动画。
- Deflate 解码仍有少量托管分配，不宣称零 GC。
- 输出是 RGBA 无损帧差，文件可能明显大于有损 MP4；这不是 H.264 的压缩率替代品。

本次实际测试数据见 `TEST_REPORT.md`。
